<%= link_to post_path(post) do%>
<%= post.title %>
<%= post.discription%>
<%= post.github_url%>
<%= post.app_url%>
<% end %>
<% if post.images.attached?%>
    <% image_first = post.images[0] %>
    <%= image_tag image_first%>
<% end %>